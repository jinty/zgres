language: python
python:
  - "3.5"
install:
    - travis_retry pip install tox
script:
    - tox
    - sudo -E env "PATH=$PATH" tox -e destroy_machine_with_sudo
notifications:
    email: false
addons:
  postgresql: "9.4"
sudo: true
